<div class='reading_list'>
    <% if (typeof bookmarklet !== 'undefined') { %>
        <a href="<%= bookmarklet.replace(/\n/g,'').replace(/\r/g,'')  %>"><%= bookmarklet_link_text || '!!' %></a>
    <% } %>
    <ol class="resource_list">
    <% resources.forEach(function(resource) { %>
        <li>
            <a href="<%= resource.entry %>" target="<%=typeof link_target === 'undefined'? '_self' : link_target %>"><h1><%= resource.title  %></h1></a>
            <div class="description"><%- resource.description %></div>
            <% if (resource.pages) { %>
                <div class="counts">
                    (<%= resource.pages.length %> pages / <%= resource.noteCount %> notes)
                </div>
                <ul class="pageControl">
                <% resource.notes.forEach(function(b) { %>
                    <li <%- b ? 'class="note"' : ''%>></li>
                <% }); %>
                </ul>
                
                <% resource.pages.forEach(function(page) { %>
                    <div class="page">
                        <%- page.description %>
                    </div>
                <% }); %>

            <% } %>
        </li>
    <% }); %>
    </ol>
</div>
